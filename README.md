# KAPTN

This is a monorepo containing the KAPTN onboarding application.

## Structure

- `/kaptn-onboarding` - Next.js application for the KAPTN onboarding experience

## Deployment

The project is deployed on Vercel. The `vercel.json` file specifies the root directory as `kaptn-onboarding` to ensure GitHub-triggered deployments build correctly.
